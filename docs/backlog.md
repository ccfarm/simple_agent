# Backlog：个人 A 股投资助手 MVP

任务级清单，配合 plan.md 使用。每个 task 完成后打勾。

## M1 项目骨架与服务启动

- [x] T1.1 初始化 Python 项目结构与虚拟环境
- [x] T1.2 编写依赖清单（FastAPI、uvicorn、SQLite 驱动等）
- [x] T1.3 搭建 FastAPI 应用入口与启动脚本（uvicorn）
- [x] T1.4 配置静态资源与模板目录，引入 Bootstrap
- [x] T1.5 实现基础页面框架与导航（设置页、股票列表页、股票详情页、AI 问答页占位）
- [x] T1.6 联调验证：本机启动服务，浏览器访问并在导航间切换

## M2 数据层与配置管理

- [x] T2.1 引入 SQLite 连接与数据库初始化（建库、会话/连接封装）
- [x] T2.2 定义数据表结构：stocks / documents / settings（含字段与时间戳）
- [x] T2.3 服务启动时自动建表（不存在则创建）
- [x] T2.4 实现 settings 读写数据访问层（按 key 存取配置）
- [x] T2.5 设置页表单：填写/保存/修改 DeepSeek API Key 与资料保存根目录
- [x] T2.6 实现设置保存与读取接口，页面加载时回填已存配置
- [x] T2.7 联调验证：自动建表成功，保存配置后重启服务仍能读取

## M3 股票列表管理

- [x] T3.1 实现 stocks 数据访问层（增 / 删 / 改 / 查，含 updated_at 维护）
- [x] T3.2 代码离线校验：6 位数字 + 代码段与市场一致性 + 库内去重
- [x] T3.3 代码在线存在性校验（公开行情接口，引入 requests）：查到则自动回填名称（只读），接口不可用/查不到则严格拒绝添加
- [x] T3.4 股票管理接口：列表查询、新增、编辑、删除
- [x] T3.5 股票列表页：渲染列表，含代码 / 名称 / 市场 / 备注
- [x] T3.6 新增与编辑股票表单（市场 SH / SZ / BJ 选择，名称由校验自动回填且只读）
- [x] T3.7 删除股票（带确认）
- [x] T3.8 详情页打通：列表点击进入详情并展示基础信息
- [x] T3.9 联调验证：校验（离线+在线）生效，增删改实时正确更新，可进入详情页

## M4 资料拉取与本地存储

- [x] T4.1 实现 documents 数据访问层（新增、按股票+类型查询、按 URL/标题判重）
- [x] T4.2 资料根目录与分类目录管理：按 `代码_名称/{reports,news,announcements}` 创建（读取设置中的根目录）
- [x] T4.3 抓取器框架：定义统一抓取接口（输入股票，输出条目列表），按类型分离实现
- [x] T4.4 接入东方财富数据源（单一数据源），分别抓取研报 / 新闻 / 公告（requests，优先 JSON 接口，含超时与编码处理）
- [x] T4.5 拉取落地：抓取条目写入本地文件 + 落 documents 表，增量去重（已存在跳过）
- [x] T4.6 详情页资料拉取卡片：类型勾选（默认全选）+「一键拉取」（同步），按钮拉取中禁用
- [x] T4.7 拉取接口与结果汇总反馈：返回各类型新增/跳过数量，失败给出明确错误
- [x] T4.8 联调验证：触发拉取后生成正确目录结构与文件，documents 落库正确，重复拉取去重生效
- [x] T4.9 拉取动画：点击「一键拉取」后展示进行中动画（AJAX 异步 + 按钮 spinner），拉取完成后动画结束并动态更新数量与结果
- [x] T4.10 资料结构化为 Markdown（利于后续 RAG chunk）：引入 pymupdf4llm 将研报/公告 PDF 解析为含标题层级与表格的 Markdown（失败回退纯文本），文件以「YAML front matter + 正文」存为 `.md`

## M5 本地资料管理

- [x] T5.1 documents 查询增强：按股票分类型读取资料列表（标题/来源/发布时间/本地路径），按时间倒序
- [x] T5.2 详情页资料列表：按研报 / 新闻 / 公告分组（Tab 或分区）展示已下载资料，显示数量
- [x] T5.3 打开本地文件：提供「打开」入口定位本地 txt/PDF（后端校验路径合法，返回文件内容/下载）
- [x] T5.4 单条资料查看：点击标题查看解析后的正文内容（读取本地 txt 渲染）
- [x] T5.5 重新拉取入口：资料列表区提供「重新拉取」（复用 AJAX 异步 + 增量去重），完成后刷新列表
- [x] T5.6 空态与异常处理：无资料友好提示；本地文件缺失时给出标注
- [x] T5.7 联调验证：资料按类型正确分组展示，可打开/查看本地文件，重新拉取后列表与数量刷新

## M6 向量索引与资料检索

- [x] T6.1 依赖与配置：引入 LlamaIndex 与本地 embedding 依赖（llama-index-embeddings-huggingface / sentence-transformers）；settings 增加本地 embedding 模型配置项与设置页表单
- [x] T6.2 索引服务模块：封装本地 embedding 构建（HuggingFaceEmbedding）、Markdown 读取与切分、按股票构建/加载本地持久化索引
- [x] T6.3 增量入库：以 documents 记录为准，仅对尚未入索引的新增 `.md` 切分入库（已索引跳过）
- [x] T6.4 集成拉取流程：拉取/重新拉取落地后自动建/更新索引，结果随拉取反馈；本地模型不可用时优雅跳过
- [x] T6.5 检索接口：提供 search(stock, query) 返回相关片段、相似度与来源文件（供 M7 复用）
- [x] T6.6 空态与异常处理：模型加载失败、索引为空时给出明确提示且不影响拉取
- [x] T6.7 联调验证：拉取后自动建立向量索引，语义检索返回相关片段与来源文件

## M7 AI 问答

- [x] T7.1 DeepSeek LLM 接入：新增 llm_service，用 LlamaIndex 的 OpenAILike LLM 封装 DeepSeek（is_chat_model=True），从 settings 读取 API Key；未配置 / 调用失败时返回明确错误
- [x] T7.2 ChatEngine 编排：复用 index_service 加载的同一 VectorStoreIndex，用 index.as_chat_engine（CondensePlusContext 模式）串起「检索 + 多轮上下文压缩 + 问答」，挂载 DeepSeek LLM 与中文系统提示
- [x] T7.3 多轮上下文：把前端传入的对话历史转为 ChatMessage 列表交给 ChatEngine，由其负责历史压缩与 query 改写；从 response.source_nodes 提取引用来源
- [x] T7.4 问答 API：新增 POST /api/stocks/{id}/ask，入参为问题与前端维护的对话历史，返回回答文本、引用资料列表（标题/类型/链接/file_path/score）与错误信息
- [x] T7.5 独立 AI 问答页：完善 /chat 页面，顶部下拉选择/切换当前股票，问答区（输入框、对话记录、发送），前端维护多轮历史并异步提交，渲染回答与引用资料（复用本地文件打开/查看入口）；详情页提供「进入 AI 问答」入口并带上当前股票
- [x] T7.6 空态与异常处理：未配置 API Key、索引为空 / 无召回、DeepSeek 报错时给出明确提示，不影响页面其他功能
- [x] T7.7 联调验证：多轮问答上下文连贯、回答标注引用资料且可打开来源文件；逐条核对 PRD 第 10 节验收标准

## M8 多用户与注册登录

- [x] T8.1 数据库 schema：新增 users 表（username 唯一、password_hash、created_at）；stocks / documents 增加 user_id，settings 改为 (user_id, key) 联合主键
- [x] T8.2 认证模块：实现密码加盐哈希与校验（passlib/hashlib），用户注册 / 按用户名查询 / 登录校验的数据访问层
- [x] T8.3 会话管理：基于签名 session cookie 维护登录态（SessionMiddleware 或等价方案），读取当前登录用户
- [x] T8.4 注册 / 登录 / 退出页面与接口：用户名 + 密码注册登录，退出清会话；用户名重复 / 密码错误等给出明确提示
- [x] T8.5 访问控制：认证依赖 / 中间件，除注册登录页与静态资源外所有页面与 API 需登录；未登录重定向登录页 / API 返回未授权
- [x] T8.6 数据访问层按用户过滤：stocks_store / documents_store / settings_store 全部带 user_id 条件；越权访问他人数据被拒绝
- [x] T8.7 文件与索引按用户分区：storage / index_service 的资料与索引目录加入用户维度（如 data/users/{user_id}/...），各用户互不覆盖
- [x] T8.8 设置页改为按用户：DeepSeek API Key 等配置按当前用户读写；llm_service / index_service 读取当前用户配置
- [x] T8.9 顶部导航：展示当前登录用户名与「退出」入口
- [x] T8.10 联调验证：两账号注册登录后股票 / 资料 / 索引 / 设置完全隔离，未登录被拦截；存量历史数据不迁移
